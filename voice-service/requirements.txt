numpy>=1.24
requests>=2.31
faster-whisper>=1.0
vosk>=0.3.44
openwakeword>=0.6.0
onnxruntime>=1.16
pytest>=8.0
# Optional experimental LiveKit wake engine:
# livekit-wakeword>=0.2.1 requires Python >=3.11. The current Mac mini
# production voice venv is Python 3.9, so install this only in a separate
# Python 3.11+ validation venv before using WAKE_ENGINE=livekit.
