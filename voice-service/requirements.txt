numpy>=1.24
requests>=2.31
faster-whisper>=1.0
vosk>=0.3.44
openwakeword>=0.6.0
onnxruntime>=1.16
pytest>=8.0
# Optional experimental LiveKit wake engine:
# livekit-wakeword>=0.2.1 requires Python >=3.11. The launchd voice service
# can stay on the existing Python 3.9 venv and call a Python 3.11 helper via
# LIVEKIT_WAKEWORD_HELPER_PYTHON.
